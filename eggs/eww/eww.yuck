; Constants
(defvar debug 0)

; Time query
(defpoll time
  :interval "10s"
  "date '+%H:%M %b %d, %Y'"
)

; Workspaces query (listener)
(deflisten workspaces "bin/workspaces")
(deflisten volume "bin/volume")
(deflisten battery "bin/battery")

; Headers
(include "bar/eww.yuck")
